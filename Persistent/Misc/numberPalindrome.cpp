#include <bits/stdc++.h>

using namespace std;

bool checkPalindrome(int key)
{
  string keyString = to_string(key);
  // then check palindrome
  // you can also reverse the digits and compare https://www.geeksforgeeks.org/check-if-a-number-is-palindrome/
}

int main()
{
  int key = 12221;
  cout << checkPalindrome(key);
}