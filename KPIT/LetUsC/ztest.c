#include <stdio.h>

int func(int a) { return a * 2; }

int main() {
  // int a = 1;
  // printf("%d %d %d\n", a, ++a, a++);

  // int *ptr = &a;
  // char ch = 'h';
  // printf("%p \n", &a);

  // int arr[3];
  // arr[6] = 7;
  // printf("%d", arr[6]);

  int max, arr[max];
  scanf("%d", &max);
}
